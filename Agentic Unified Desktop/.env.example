# Azure OpenAI / LLM configuration

# Example local dev values (DO NOT COMMIT real keys)
# ENDPOINT_URL="https://sushgenai.openai.azure.com/"
# DEPLOYMENT_NAME="gpt-4o"
# AZURE_OPENAI_API_KEY="<redacted-example>"

# Microsoft 365 / Graph (service principal)
# Use tenant domain; GUID can be discovered via the MCP tool `graph.discoverTenant`.
AZURE_TENANT_DOMAIN=your-tenant.onmicrosoft.com
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# Optional
# ALLOW_RETURN_PASSWORD=false
